# DM-Diff 达梦数据库对比工具规划书

## 1. 运行环境与技术栈
- **物理环境**：客户内网，完全离线环境
- **目标数据库**：达梦数据库（DM8）
- **开发技术**：Java 21 + Spring Boot 3.x + Bootstrap 5
- **交付产物**：内置绿色版 JRE 21 的独立运行 Jar 包（免安装）

## 2. 核心功能模块
- **连接管理**：
  - 支持配置源库（旧库）与目标库（新库）。
  - 支持一键测试连接，回显数据库版本及大小写敏感参数。
  - 增加连接超时设置，避免长时间等待。
- **全量看板**：
  - 比对前展示双方表、视图、索引、存储过程的总数差。
  - 支持更多统计维度（如触发器、分区表）。
- **结构比对**：
  - 表比对（新增/删除）。
  - 字段比对（新增/删除、类型变化、长度变化、是否为空、默认值、注释、字段顺序变化）。
  - 索引比对（唯一、聚集、普通索引及字段顺序）。
  - 提供更详细的差异报告。
- **容错过滤**：
  - 支持忽略表名/字段名大小写（针对.NET转Java痛点）。
  - 支持配置表名黑名单（如过滤 *_BAK 表）。
  - 明确黑名单匹配规则（正则 vs 通配符）。
- **脚本生成**：
  - 自动生成正向升级与逆向回滚 SQL，且严格遵循 DDL 执行顺序（先删约束索引 -> 删字段 -> 增表字段 -> 增索引约束）。
  - 支持生成更复杂的 DDL（如分区表、外键约束）。

## 3. 交付目录结构
- **/jre21**：绿色版 Java 环境。
- **/lib**：内置 DmJdbcDriver8.jar。
- **/logs**：离线错误日志落盘。
- **start.sh / start.bat**：一键启动脚本，支持检测 JRE 是否存在。
- **dm-diff-tool.jar**：主程序。

## 4. 测试与验证
- **单元测试**：
  - 覆盖核心模块（如 DiffService 和 SqlGeneratorService）。
  - 提供离线测试数据，模拟不同数据库版本的差异。
- **日志验证**：
  - 确保日志记录完整，支持离线排查。
  - 增加日志清理功能，避免日志目录无限增长。